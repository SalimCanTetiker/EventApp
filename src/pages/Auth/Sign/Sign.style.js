import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: 'white',
        flex: 1,
      },
      register_container: {
        flexDirection: 'row',
        alignItems: 'center',
        marginTop: 40,
      },
      register_title: {
        fontSize: 15,
      },
      register_button: {
        fontSize: 17,
        fontWeight: 'bold',
      },
})